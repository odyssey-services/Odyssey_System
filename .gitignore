node_modules/
__pycache__/
.DS_Store

# Local-only Owlbear dev manifest (points at http://127.0.0.1:3000).
# Production deployment uses manifest.json (GitHub Pages). Never deploy this.
manifest.dev.json
